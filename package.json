{
  "name": "nvidia-free-models",
  "version": "1.0.0",
  "description": "Access 5 frontier Chinese AI models for free via NVIDIA NIM",
  "type": "module",
  "scripts": {
    "start": "npx tsx agent/cli.ts",
    "test": "vitest",
    "test:models": "vitest run model.config.test.ts",
    "test:agent": "vitest run agent.test.ts",
    "test:tools": "vitest run tools.test.ts",
    "test:all": "vitest run"
  },
  "dependencies": {
    "openai": "^4.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "tsx": "^4.22.4",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
